<UserControl x:Class="Idibri.RevitPlugin.Common.UserControls.Spinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="25">
  <UserControl.Resources>

  </UserControl.Resources>
  <Grid Margin="0">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Button x:Name="IncrementButton" Grid.Column="0" Grid.Row="0" Padding="2,1,2,1" Click="IncrementButton_OnClick" Margin="0">
      <Path Fill="Black" Height="4" Width="8" Data="F1 M 0,4 8,4, 4,0 Z" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Button>
    <Button x:Name="DecrementButton" Grid.Column="0" Grid.Row="1" Padding="2,1,2,1" Click="DecrementButton_OnClick" Margin="0">
      <Path Fill="Black" Height="4" Width="8" Data="F1 M 0,0 8,0, 4,4 Z" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Button>
  </Grid>
</UserControl>
